import { BlockStyleProps } from '../../types/props';
import { ComponentPropsWithoutRef, CSSProperties } from 'react';
interface BlockProps extends BlockStyleProps {
    className?: string;
    style?: CSSProperties;
    id?: string;
    title?: string;
    noDefaultStyle?: boolean;
}
interface HTMLBlockProps extends Omit<ComponentPropsWithoutRef<'div'>, keyof BlockProps> {
}
interface Props extends BlockProps {
    htmlAttributes?: HTMLBlockProps;
}
declare const Block: ({ className, style, title, id, noDefaultStyle, htmlAttributes, ...restWrapperProps }: Props) => import("react/jsx-runtime").JSX.Element;
export default Block;
