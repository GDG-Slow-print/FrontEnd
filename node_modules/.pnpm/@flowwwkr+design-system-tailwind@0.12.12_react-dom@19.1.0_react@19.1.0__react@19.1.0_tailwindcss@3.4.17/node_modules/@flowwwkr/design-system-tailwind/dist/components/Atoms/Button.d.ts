import { ComponentPropsWithoutRef, CSSProperties } from 'react';
import { ButtonStyleProps } from '../../types/props';
interface ButtonProps extends ButtonStyleProps {
    className?: string;
    style?: CSSProperties;
    id?: string;
    title?: string;
    children?: React.ReactNode;
    type?: 'button' | 'submit';
    theme?: 'filled' | 'outlined' | 'ghost' | 'plain';
    onClick?: React.MouseEventHandler<HTMLButtonElement>;
    disabled?: boolean;
    noDefaultStyle?: boolean;
    hover?: boolean;
}
interface HTMLButtonProps extends Omit<ComponentPropsWithoutRef<'button'>, keyof ButtonProps> {
}
interface Props extends ButtonProps {
    htmlAttributes?: HTMLButtonProps;
}
declare const Button: ({ className, style, title, id, type, children, theme, onClick, disabled, noDefaultStyle, hover, htmlAttributes, ...restButtonProps }: Props) => import("react/jsx-runtime").JSX.Element;
export default Button;
