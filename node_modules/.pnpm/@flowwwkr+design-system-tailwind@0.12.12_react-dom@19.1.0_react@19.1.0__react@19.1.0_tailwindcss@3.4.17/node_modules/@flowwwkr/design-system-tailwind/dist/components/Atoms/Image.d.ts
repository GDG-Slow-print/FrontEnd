import { ImageStyleProps } from '../../types/props';
import { ComponentPropsWithoutRef, CSSProperties } from 'react';
interface ImageProps extends ImageStyleProps {
    className?: string;
    style?: CSSProperties;
    id?: string;
    title?: string;
    src: string;
    alt: string;
    srcSet?: string;
    sizes?: string;
    loading?: 'eager' | 'lazy';
    decoding?: 'sync' | 'async' | 'auto';
}
interface HTMLImageProps extends Omit<ComponentPropsWithoutRef<'img'>, keyof ImageProps> {
}
interface Props extends ImageProps {
    htmlAttributes?: HTMLImageProps;
}
declare const Image: ({ className, style, title, id, src, alt, srcSet, sizes, loading, decoding, htmlAttributes, ...restImgProps }: Props) => import("react/jsx-runtime").JSX.Element;
export default Image;
