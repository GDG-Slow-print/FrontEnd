import { SpanStyleProps } from '../../types/props';
import { ComponentPropsWithoutRef, CSSProperties } from 'react';
interface SpanProps extends SpanStyleProps {
    children: React.ReactNode;
    className?: string;
    style?: CSSProperties;
    id?: string;
    title?: string;
    noDefaultStyle?: boolean;
}
interface HTMLSpanProps extends Omit<ComponentPropsWithoutRef<'span'>, keyof SpanProps> {
}
interface Props extends SpanProps {
    htmlAttributes?: HTMLSpanProps;
}
declare const Span: ({ children, className, style, title, id, noDefaultStyle, htmlAttributes, ...restTextProps }: Props) => import("react/jsx-runtime").JSX.Element;
export default Span;
