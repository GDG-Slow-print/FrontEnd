import { TextStyleProps } from '../../types/props';
import { ComponentPropsWithoutRef, CSSProperties } from 'react';
interface TextProps extends TextStyleProps {
    children: React.ReactNode;
    className?: string;
    style?: CSSProperties;
    id?: string;
    title?: string;
    tag?: 'p' | 'span' | 'div' | 'strong' | 'em';
    noDefaultStyle?: boolean;
}
interface HTMLTextProps extends Omit<ComponentPropsWithoutRef<'p'>, keyof TextProps> {
}
interface Props extends TextProps {
    htmlAttributes?: HTMLTextProps;
}
declare const Text: ({ children, className, style, title, id, tag, noDefaultStyle, htmlAttributes, ...restTextProps }: Props) => import("react/jsx-runtime").JSX.Element;
export default Text;
