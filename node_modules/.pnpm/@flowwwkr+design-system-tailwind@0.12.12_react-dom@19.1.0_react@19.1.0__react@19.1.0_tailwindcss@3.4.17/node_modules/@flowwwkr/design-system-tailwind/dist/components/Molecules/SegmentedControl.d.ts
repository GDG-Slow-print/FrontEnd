import { BorderStyle } from '../../types/style';
interface Props {
    segments: {
        [key: string]: string;
    };
    currentSegment: string;
    handleSegmentChange: (segment: string) => void;
    borderRadius?: BorderStyle['borderRadius'];
}
declare const SegmentedControl: ({ segments, currentSegment, handleSegmentChange, borderRadius, }: Props) => import("react/jsx-runtime").JSX.Element;
export default SegmentedControl;
