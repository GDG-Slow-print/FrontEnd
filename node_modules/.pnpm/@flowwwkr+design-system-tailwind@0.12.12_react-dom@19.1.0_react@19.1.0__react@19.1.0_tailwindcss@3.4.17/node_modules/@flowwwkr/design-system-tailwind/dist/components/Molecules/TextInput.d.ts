import { FlexStyleProps, InputStyleProps } from '../../types/props';
export interface TextInputProps {
    /**
     * ID attribute for the input element.
     * A unique identifier for the input field.
     */
    id?: string;
    /**
     * Name attribute for the input element.
     * Used to reference the form data after submission.
     */
    name?: string;
    /**
     * Specifies whether the input field is required.
     * If true, the field must be filled out before form submission.
     */
    required?: boolean;
    /**
     * Placeholder text displayed in the input field when it is empty.
     */
    placeholder?: string;
    /**
     * The current value of the TextInput.
     */
    value?: string;
    /**
     * The default value of the TextInput (uncontrolled).
     * Used to set the initial value before the user interacts with it.
     * If `value` is also provided, `defaultValue` is ignored.
     */
    defaultValue?: string;
    /**
     * Size of the TextInput.
     * Controls the height and padding of the component.
     * @default 'md'
     */
    size?: 'sm' | 'md' | 'lg';
    /**
     * Color variant of the TextInput.
     * Determines the primary theme color of the component.
     * @default 'neutral'
     */
    color?: 'primary' | 'secondary' | 'tertiary' | 'neutral';
    /**
     * Visual state of the TextInput.
     * Used to represent feedback such as errors or success.
     * @default 'default'
     */
    state?: 'default' | 'error' | 'warning' | 'success' | 'perfect';
    /**
     * Theme variant of the TextInput.
     * Defines the visual style of the component (e.g., filled or outlined).
     * @default 'outlined'
     */
    theme?: 'filled' | 'outlined';
    /**
     * Indicates whether the TextInput field is disabled.
     * If true, the field is non-interactive and visually styled as disabled.
     */
    disabled?: boolean;
    /**
     * Style properties for the container of the TextInput (root element).
     * Should follow the <Flex> component style conventions.
     */
    rootStyle?: FlexStyleProps;
    /**
     * Style properties for the input field itself.
     * Should follow the <Input> component style conventions.
     */
    inputStyle?: InputStyleProps;
    /**
     * Callback function triggered when the value of the TextInput changes.
     * @param {string} value - The new value of the TextInput.
     */
    onChange?: (value: string) => void;
}
/**
 * The TextInput Component for creating a text input field with a toggleable visibility feature.
 *
 * **Structure**:
 * ```tsx
 * <Root>
 *   <Input />
 * </Root>
 * ```
 *
 * **Props**:
 * @param {object} props - The properties object for the TextInput component.
 * @param {string} [props.name] - The `name` attribute of the input field, used to identify the input in forms.
 * @param {boolean} [props.required] - Specifies whether the field is required. If `true`, the input must be filled out before form submission.
 * @param {string} [props.placeholder] - Placeholder text displayed inside the input field when it is empty.
 * @param {string} [props.value] - The current value of the input field. This makes the component controlled when provided.
 * @param {'sm' | 'md' | 'lg'} [props.size] - The size of the TextInput, which controls the height and padding. Defaults to `'md'`.
 * @param {'primary' | 'secondary' | 'tertiary' | 'neutral'} [props.color] - The color variant of the TextInput. Defaults to `'neutral'`.
 * @param {'default' | 'error' | 'warning' | 'success' | 'perfect'} [props.state] - The visual state of the TextInput for feedback purposes. Defaults to `'default'`.
 * @param {'filled' | 'outlined'} [props.theme] - The theme of the TextInput. Determines the visual style. Defaults to `'outlined'`.
 * @param {boolean} [props.disabled] - Indicates whether the TextInput is disabled. If `true`, the input field is non-interactive.
 * @param {FlexStyleProps} [props.rootStyle] - Style properties applied to the root `<Flex>` element, which wraps the input and toggle button.
 * @param {InputStyleProps} [props.inputStyle] - Style properties applied directly to the `<Input>` element.
 * @param {Function} [props.onChange] - Callback function triggered when the input value changes.
 *   @param {string} value - The updated value of the TextInput.
 */
declare const TextInput: ({ id, name, required, value, defaultValue, placeholder, size, color, state, theme, onChange, rootStyle, inputStyle, disabled, }: TextInputProps) => import("react/jsx-runtime").JSX.Element;
export default TextInput;
