import { WrapperStyleProps, TextStyleProps } from '../../index';
import { IconProps } from '../Atoms/Icon';
type TooltipProps = {
    position?: 'right-top' | 'right-middle' | 'right-bottom' | 'left-top' | 'left-middle' | 'left-bottom' | 'top-right' | 'top-middle' | 'top-left' | 'bottom-right' | 'bottom-middle' | 'bottom-left';
    content: string | React.ReactNode;
    size?: 'sm' | 'md' | 'lg';
    iconProps?: IconProps;
    wrapperColor?: WrapperStyleProps['color'];
    wrapperStyle?: Omit<WrapperStyleProps, 'color'>;
    textStyle?: TextStyleProps;
};
/**
 * ## Tooltip 컴포넌트
 * ***
 * ### 구조
 * ```tsx
 * <Flex>
 *   <IconInfo />
 *   <Wrapper>
 *     <Text />
 *   </Wrapper>
 * </Flex>
 * ```
 * ***
 * ### TooltipPosition
 * - right-top
 * - right-middle
 * - right-bottom
 * - left-top
 * - left-middle
 * - left-bottom
 * - top-right
 * - top-middle
 * - top-left
 * - bottom-right
 * - bottom-middle
 * - bottom-left
 * ***
 * ### Props
 * @param {TooltipProps.position} props.position - 툴팁 위치
 * @param {string} props.content - 툴팁 내용
 * @param {'sm' | 'md' | 'lg'} props.size - 아이콘 크기
 * @param {IconProps} props.iconProps - 아이콘 프롭스
 * @param {WrapperStyleProps['color']} props.wrapperColor - 툴팁 배경색
 * @param {Omit<WrapperStyleProps, 'color'>} props.wrapperStyle - 툴팁 스타일 (color 제외)
 * @param {TextStyleProps} props.textStyle - 툴팁 텍스트 스타일
 * @returns
 */
declare const Tooltip: ({ position, content, size, iconProps, wrapperColor, wrapperStyle, textStyle, }: TooltipProps) => import("react/jsx-runtime").JSX.Element;
export default Tooltip;
