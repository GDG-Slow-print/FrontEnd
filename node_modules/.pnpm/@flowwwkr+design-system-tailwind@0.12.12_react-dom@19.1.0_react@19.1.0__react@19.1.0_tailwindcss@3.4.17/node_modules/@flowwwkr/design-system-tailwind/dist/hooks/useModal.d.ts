interface Params {
    defaultOpen?: boolean;
}
declare const useModal: ({ defaultOpen }: Params) => {
    openModal: () => void;
    closeModal: () => void;
    isModalOpen: boolean;
};
export default useModal;
