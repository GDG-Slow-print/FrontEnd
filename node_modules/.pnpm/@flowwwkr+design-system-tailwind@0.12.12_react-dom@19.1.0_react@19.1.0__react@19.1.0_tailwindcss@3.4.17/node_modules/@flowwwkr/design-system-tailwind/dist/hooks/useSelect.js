import { useState as n } from "react";
const S = ({
  defaultOption: t
}) => {
  const [c, e] = n(t);
  return {
    selectedOption: c,
    setSelectedOption: e,
    handleChangeSelect: (s) => {
      e(s);
    },
    resetSelect: () => {
      e(t);
    }
    // 선택값 초기화 함수 추가
  };
};
export {
  S as default
};
