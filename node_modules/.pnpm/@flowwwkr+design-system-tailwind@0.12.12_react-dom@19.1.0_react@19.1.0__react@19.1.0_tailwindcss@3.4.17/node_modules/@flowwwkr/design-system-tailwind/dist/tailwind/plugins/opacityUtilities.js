import t from "tailwindcss/plugin";
import { opacityTheme as p } from "../themes/index.js";
const m = t(function({ matchUtilities: o }) {
  o(
    {
      oc: (i) => ({
        opacity: i
      })
    },
    {
      values: p
    }
  );
});
export {
  m as opacityUtilities
};
