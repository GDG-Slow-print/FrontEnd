import i from "tailwindcss/plugin";
import { positioningTheme as e } from "../themes/index.js";
const s = i(function({ matchUtilities: o }) {
  o(
    {
      t: (t) => ({
        top: t
      }),
      r: (t) => ({
        right: t
      }),
      b: (t) => ({
        bottom: t
      }),
      l: (t) => ({
        left: t
      })
    },
    {
      values: e,
      supportsNegativeValues: !0
    }
  );
});
export {
  s as positionUtilities
};
