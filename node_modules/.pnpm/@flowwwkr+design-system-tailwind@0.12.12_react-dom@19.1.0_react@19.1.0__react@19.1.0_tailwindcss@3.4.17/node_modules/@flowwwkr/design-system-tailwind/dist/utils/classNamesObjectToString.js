const s = (o, i) => {
  let r = "";
  return Object.entries(o).forEach((t) => {
    t[1] && typeof t[1] == "string" ? r += `${t[1]} ` : typeof t[1] == "object" && (r += `${s(t[1])} ` || "");
  }), r.trim();
};
export {
  s as default
};
